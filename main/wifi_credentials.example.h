#ifndef WIFI_CREDENTIALS_H
#define WIFI_CREDENTIALS_H

/* Copy this file to wifi_credentials.h (gitignored) and fill in your
 * Wi-Fi network. main.c includes wifi_credentials.h if it exists,
 * otherwise falls back to the placeholder constants below — the
 * firmware will still build but can't actually associate. */

#define WIFI_SSID     "your-ssid-here"
#define WIFI_PASSWORD "your-wifi-password-here"

#endif /* WIFI_CREDENTIALS_H */
