/* Smoke test: confirm both libraries link and run. Real coverage is in
 * test_encoder, test_status_decode, test_packbits, test_bitmap,
 * test_oracle_fixtures, test_encoder_vs_oracle, test_mock_transport. */

#include "pt_protocol.h"
#include "pt_transport.h"

int main(void) { return 0; }
